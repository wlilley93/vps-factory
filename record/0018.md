# Record 0018

The deferred-verification ledger is discharged. Lean 4.15.0 (arm64-apple-darwin23.6.0,
commit 11651562caae — the pinned toolchain) executed every entry queued on 2026-08-22.

`lake build Vps` is green: World → Instrument → Genesis → Legitimacy → Gate → Precedent →
Proofs → Book → Examples, with no `sorry` and no `axiom`. `book_lawful` type-checks over
the live 21-instrument book, so the sixteen enactments [2026] VPS 6–21 — filed against
TypeScript pre-checks while Lean was unreachable — are law the kernel has now accepted.
Per §21.5 an enactment the kernel rejects never happened; none was rejected.

Vendoring defect, repaired: the M11 copy took the nine modules of `kernel/Vps/` but not the
root aggregator `kernel/Vps.lean`, so `lean_lib Vps` had no root and the library could never
have built. `lean/Vps.lean` is restored byte-identical from canonical. `lean/Foundry.lean`
is added as Foundry's own root; it deliberately does not import `Foundry/Cases/`, whose
generated files carry the theorem under test and must fail to compile whenever a case is RED.

Codegen defect, repaired: the requirement's Prop wrapper was emitted as `def`, which is not
reducible, so typeclass resolution could not see through it to the underlying Bool equality
and rung-1 `decide` failed with "failed to synthesize Decidable (…)" on goals that are in
fact decidable. Every generated case file had been failing to compile for this reason. It is
now `abbrev`. This defeated §1.3's whole Bool-valued-predicate design and was invisible
while Lean was deferred — exactly what Protocol 5b exists to catch.

Gate evaluation now runs in Lean, not the mirror: a kernel edit without a record is denied
citing [2026] VPS 2, the same edit with a record is allowed, and a `gate/` edit is denied
citing [2026] VPS 3. Every denial named its instrument.

The example vectors use `native_decide` and therefore put the compiler in the trusted base
(Charter Art. 8). They pass on arm64-apple-darwin; PROVENANCE.md previously recorded only
x86_64-unknown-linux-gnu, and now records both.
